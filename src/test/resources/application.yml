spring:
  datasource:
    url: jdbc:h2:mem:employee_management;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: TestSecretKeyForJwtSigningTestSecretKeyForJwtSigningTestSecretKey
  expiration-ms: 900000
  refresh-expiration-ms: 86400000
